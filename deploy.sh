#!/bin/bash
forever stopall

# If you are using git
# git pull

# PRO mode
# forever start index.js -p -g <SERVER_ID> -t <BOT_TOKEN_ID>

# Normal mode
# forever start index.js -g <SERVER_ID> -t <BOT_TOKEN_ID>
