#!/bin/bash
# node deploy-commands.js -c <BOT_CLIENT_ID> -g <SERVER_ID> -t <BOT_TOKEN_ID>

# PRO mode
# nodemon index.js -p -g <SERVER_ID> -t <BOT_TOKEN_ID>

# Normal mode
# nodemon index.js -g <SERVER_ID> -t <BOT_TOKEN_ID>
